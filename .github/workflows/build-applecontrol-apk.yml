name: Build G991B Apple Control v1.3 GPU MIF APK

on:
  workflow_dispatch:
  push:
    branches: [ "apple-control-v13-gpumif" ]
    paths:
      - 'applecontrol/**'
      - 'settings.gradle'
      - '.github/workflows/build-applecontrol-apk.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Build Apple Control debug APK
        run: gradle :applecontrol:assembleDebug --stacktrace

      - name: Rename APK
        run: cp applecontrol/build/outputs/apk/debug/applecontrol-debug.apk 'G991B Apple Control v1.3.apk'

      - uses: actions/upload-artifact@v4
        with:
          name: G991B-Apple-Control-v1.3-APK
          path: 'G991B Apple Control v1.3.apk'
          if-no-files-found: error
